export interface Item {
    id: number;     // ID del elemento (ajusta el tipo si es necesario)
    name: string;   // Nombre del elemento (ajusta según lo que esperas de la API)
    // Añade más propiedades según la estructura de tu objeto de la API
  }
  